{% for product in products %}

    <div class="container">
        <div class="row">
            {# img #}
            <h3>{{ product.title }}</h3>
            <div class="control-btn"></div>
            <p>
                {# {{ product.description }} #}
                Lorem ipsum dolor sit amet, consectetur adipisicing elit. Accusamus ad aperiam architecto aut autem
                beatae
                consequatur cumque ducimus ea eo
            </p>
            <div class="properties">
                <div class="qty">
                    <h4>Stock dispo</h4>
                    <span class="number">{{ product.quantity }}</span>
                </div>
                <div class="price">
                    <h4>Prix</h4>
                    <span class="price-inr">
        <i class="fa fa-inr"></i>
        <span class="amount">{{ product.price }} €</span>
      </span>
                </div>
                <div class="delivery">
                    <h4>Livraison</h4>
                    <i class="fa fa-fighter-jet"></i>
                    <span class="time">Livraison ou A emporter</span>
                </div>
            </div>
            <input class="ip-add-cart" type="button" value="Add to cart"/>
        </div>
    </div>

{% endfor %}

<script src="https://unpkg.com/axios/dist/axios.min.js"></script>
<script src="{{ asset('js/addcart.js') }}"></script>